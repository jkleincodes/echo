# ──────────────────────────────────────────────
# Echo — Environment Variables
# Copy this file to .env and fill in the values.
# ──────────────────────────────────────────────

# ── Required ──────────────────────────────────

# Public IP address for WebRTC (voice/video). On a VPS, use the server's
# public IP. For local development, use 127.0.0.1.
MEDIASOUP_ANNOUNCED_IP=127.0.0.1

# ── Optional ──────────────────────────────────

# JWT signing secret. If not set, one is auto-generated and persisted to
# server/data/.jwt_secret on first run.
# JWT_SECRET=

# Public URL where your instance is reachable (used in emails, invite links).
# Default: http://localhost:3001
# APP_URL=https://echo.example.com

# Email sender address (requires RESEND_API_KEY to actually send).
# Default: Echo <noreply@localhost>
# EMAIL_FROM=Echo <noreply@echo.example.com>

# Resend API key for transactional emails (password reset, verification).
# Leave empty to disable email features.
# RESEND_API_KEY=

# Giphy API key for GIF search. Leave empty to disable.
# GIPHY_API_KEY=

# ── HTTPS (Caddy) ────────────────────────────
# To enable automatic HTTPS, run:
#   docker compose --profile https up -d

# Your domain name. Caddy will auto-provision a TLS certificate.
# DOMAIN=echo.example.com

# ── Client Updates ────────────────────────────

# Current app version advertised to Electron clients.
# APP_VERSION=1.0.0

# URL where .dmg/.exe builds are hosted. Empty disables update prompts.
# APP_DOWNLOAD_URL=

# Release notes shown in the update notification.
# APP_RELEASE_NOTES=
